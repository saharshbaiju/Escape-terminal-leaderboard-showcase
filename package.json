{
  "name": "escape-terminal-leaderboard-tv",
  "version": "1.0.0",
  "private": true,
  "description": "Realtime leaderboard showcase for a stall TV — Escape the Terminal.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
